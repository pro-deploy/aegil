# Зависимости тренера классификатора маршрутизации SetFit сервиса разбора первопричин
# aegil. Обучение по умолчанию на центральном процессоре: колесо torch (CPU)
# ставится отдельным шагом в Dockerfile до этих зависимостей и там же запинено, чтобы
# пакет setfit не притянул тяжёлую сборку с поддержкой CUDA.
#
# Все версии закреплены точно (==), а не диапазонами, ради воспроизводимости сборки:
# по требованию ревизии образ тренера должен собираться детерминированно, чтобы по
# одному тегу образа всегда было ясно, какой именно набор зависимостей в нём работает.
#
# Верхняя граница transformers по-прежнему обязательна и здесь соблюдена точным пином.
# Начиная с релиза transformers 5.0 из модуля transformers.training_args удалена функция
# default_logdir, на которую опирается внутренний модуль setfit.training_args, поэтому на
# transformers 5.x обучение упало бы на импорте setfit внутри train_model (сбой при этом
# отражается ненулевым кодом выхода и строкой training failed). Диапазон совместимости
# setfit требует transformers не ниже 4.41; выбранная версия удовлетворяет этому.
transformers==4.44.2
setfit==1.1.0
datasets==2.21.0
sentence-transformers==3.0.1
scikit-learn==1.5.1
psycopg2-binary==2.9.9
boto3==1.34.162
