fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
# Запись размеченных примеров маршрутизации в Postgres (активное обучение).
psycopg2-binary>=2.9
# Инференс обученного классификатора маршрутизации SetFit и синхронизация модели из
# S3 (её обучает и выкладывает тренер services/rca-trainer). torch ставится CPU-колесом
# в Dockerfile до этих зависимостей. При отсутствии модели, мягкая деградация к
# ключевому фолбэку и эскалации к Gemma 4.
# Версия transformers обязательно закрепляется ниже пятой мажорной. Начиная с релиза
# transformers 5.0 из модуля transformers.training_args удалена функция default_logdir,
# на которую опирается внутренний модуль setfit.training_args, поэтому без верхней границы
# пакет setfit вовсе не импортируется (сборка иначе тянет transformers 5.x, и загрузка
# классификатора молча деградирует к ключевому фолбэку). Диапазон согласован со всем
# стеком: setfit требует transformers не ниже 4.41, sentence-transformers допускает
# трансформеры от 4.41 включительно и ниже 6.0.
transformers>=4.41,<5
setfit>=1.0
boto3>=1.34
